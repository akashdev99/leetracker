import 'package:flutter/material.dart';

class Listing extends StatelessWidget {
  final List<dynamic> items;
  const Listing({super.key, required this.items});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
